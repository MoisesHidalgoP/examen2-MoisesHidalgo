export interface Portero {
    id: string;
    data: {
      nombre: string;
      apellidos: string;
      ciudad: string;
      telefono: string;
      email: string;
      mesDisponible: string;
    }
}
